<Styles
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style Selector="TextBlock.settingsTabTextBlock">
        <Setter Property="FontFamily" Value="SansSerif" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="Foreground" Value="{DynamicResource SettingsDialogForegroundBrush}" />
    </Style>

    <Style Selector="Button.dialogButton">
        <Setter Property="FontSize" Value="18" />
        <Setter Property="Margin" Value="20,10" />
        <Setter Property="Padding" Value="12,5" />
        <Setter Property="Background" Value="{DynamicResource HighlightBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource DialogButtonForegroundBrush}" />
    </Style>

    <Style Selector="Button.dialogButton /template/ ContentPresenter">
        <Setter Property="CornerRadius" Value="6" />
    </Style>

    <Style Selector="Button.dialogButton:pointerover">
        <Setter Property="Background" Value="{DynamicResource ThemeAccentBrush3}" />
    </Style>

    <Style Selector="Button.dialogButton:pressed /template/ ContentPresenter">
        <Setter Property="Background" Value="{DynamicResource DialogButtonPressedBrush}" />
    </Style>

    <Style Selector="TextBlock.dialogTextBlock">
        <Setter Property="FontFamily" Value="SansSerif" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="Margin" Value="0,5" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

    <Style Selector="TextBlock.overWriteDialogTextBlock">
        <Setter Property="FontSize" Value="18" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

    <Style Selector="TextBlock.openWithDialogTextBlock">
        <Setter Property="FontSize" Value="18" />
    </Style>

    <Style Selector="TextBlock.boldStyleTextBlock">
        <Setter Property="FontWeight" Value="Bold" />
    </Style>

    <Style Selector="Border.settingsContentBorder">
        <Setter Property="BorderThickness" Value="1,0,0,0" />
        <Setter Property="BorderBrush" Value="{DynamicResource SettingsTabBorderBrush}" />
    </Style>

    <Style Selector="TextBox.settingsTextBox">
        <Setter Property="Margin" Value="5,10,0,0" />
    </Style>

    <Style Selector="TabControl.propertiesDialogTabControl">
        <Setter Property="Padding" Value="15" />
    </Style>

    <Style Selector="TabControl.propertiesDialogTabControl TabItem">
        <Setter Property="Padding" Value="5" />
        <Setter Property="HeaderTemplate">
            <Setter.Value>
                <DataTemplate>
                    <StackPanel Orientation="Vertical">
                        <Border Classes="tabBorder">
                            <Grid Classes="inactiveTabGrid" Height="40">
                                <TextBlock Classes="tabNameTextBlock inactiveTabNameTextBlock"
                                           Text="{Binding Path=Header, RelativeSource={RelativeSource AncestorType=TabItem}}" />
                            </Grid>
                        </Border>

                        <Rectangle Classes="tabRectangle" Height="2" />
                    </StackPanel>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style Selector="TabControl.propertiesDialogTabControl TabItem:selected">
        <Setter Property="HeaderTemplate">
            <Setter.Value>
                <DataTemplate>
                    <StackPanel Orientation="Vertical">
                        <Border Classes="tabBorder">
                            <Grid Height="40">
                                <TextBlock Classes="tabNameTextBlock"
                                           Text="{Binding Path=Header, RelativeSource={RelativeSource AncestorType=TabItem}}" />
                            </Grid>
                        </Border>

                        <Rectangle Classes="tabRectangle tabRectangleGloballyActive"
                                   Height="2" />
                    </StackPanel>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style Selector="Button.alternativeColorDialogButton">
        <Setter Property="Background" Value="{DynamicResource OperationButtonHoverBackgroundColorBrush}" />
    </Style>

    <Style Selector="Button.alternativeColorDialogButton:pointerover">
        <Setter Property="Background" Value="{DynamicResource OperationButtonBackgroundColorBrush}" />
    </Style>

    <Style Selector="Button.alternativeColorDialogButton:pressed /template/ ContentPresenter">
        <Setter Property="Background" Value="{DynamicResource OperationButtonPressedBackgroundColorBrush}" />
    </Style>

    <Style Selector="TextBox#NodeNameTextBox">
        <Setter Property="Margin" Value="0,10" />
        <Setter Property="Width" Value="300" />
    </Style>

    <Style Selector="Button.applyDialogButton">
        <Setter Property="Background" Value="{DynamicResource SuccessBackgroundBrush}" />
    </Style>

    <Style Selector="Button.applyDialogButton:pointerover">
        <Setter Property="Background" Value="{DynamicResource SuccessHoverBackgroundBrush}" />
    </Style>

    <Style Selector="Button.applyDialogButton:pressed /template/ ContentPresenter">
        <Setter Property="Background" Value="{DynamicResource SuccessPressedBackgroundBrush}" />
    </Style>

    <Style Selector="TabItem Border.settingsTabHeaderBorder">
        <Setter Property="Padding" Value="5,5,10,5" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="Margin" Value="0,0,10,0" />
        <Setter Property="Width" Value="150" />
    </Style>

    <Style Selector="TabItem:selected Border.settingsTabHeaderBorder">
        <Setter Property="Background" Value="{DynamicResource HighlightBrush}" />
    </Style>

    <Style Selector="Button.transparentDialogButton">
        <Setter Property="Background" Value="{DynamicResource ThemeBackgroundBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="{DynamicResource TransparentButtonBorderBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource ThemeForegroundBrush}" />
    </Style>

    <Style Selector="Button.iconDialogButton">
        <Setter Property="Background" Value="{DynamicResource OperationButtonBackgroundColorBrush}" />
        <Setter Property="Padding" Value="10,0" />
    </Style>

    <Style Selector="Button.transparentDialogButton:pointerover /template/ ContentPresenter">
        <Setter Property="BorderBrush" Value="{DynamicResource ThemeForegroundBrush}" />
    </Style>

    <Style Selector="Button.transparentDialogButton:pointerover">
        <Setter Property="Foreground" Value="{DynamicResource HighlightForegroundBrush}" />
    </Style>

    <Style Selector="Button.transparentDialogButton:pressed /template/ ContentPresenter">
        <Setter Property="Background" Value="{DynamicResource DialogButtonPressedBrush}" />
        <Setter Property="BorderBrush" Value="Transparent" />
    </Style>

    <Style Selector="Button.topOperationButton:pressed /template/ ContentPresenter">
        <Setter Property="Background" Value="Transparent" />
    </Style>

</Styles>