<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:mainWindow="clr-namespace:Camelot.ViewModels.MainWindow"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="Camelot.Views.Main.OperationsView">

    <Design.DataContext>
        <mainWindow:OperationsViewModel />
    </Design.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Button Classes="operationButton" Content="Edit" Command="{Binding EditCommand}" />
        <Button Classes="operationButton" Grid.Column="1" Content="Copy" Command="{Binding CopyCommand}" />
        <Button Classes="operationButton" Grid.Column="2" Content="Move" Command="{Binding MoveCommand}" />
        <Button Classes="operationButton" Grid.Column="3" Content="New folder" Command="{Binding NewFolderCommand}"  />
        <Button Classes="operationButton" Grid.Column="4" Content="Remove" Command="{Binding RemoveCommand}" />
    </Grid>
</UserControl>
